
import React from "react";

const Header = () => {
  const style = {
    "textAlign": "center"
  };

  return (
    <div style={style}>
      <h1>Grow the web with referrals!</h1>
    </div>
  );
};

export default Header;

